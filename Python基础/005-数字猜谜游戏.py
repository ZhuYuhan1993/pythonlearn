"""
项目5：数字猜谜游戏
### 基础要求
1. 程序随机生成1-100的数字
2. 用户有10次猜测机会
3. 每次提示"太大"、"太小"或"正确"
4. 游戏结束后显示猜测次数
### 完整要求
1. 添加难度级别（不同数字范围）
2. 计时功能（猜测用时）
3. 积分系统（根据表现评分）
4. 保存最高分记录
### 扩展要求
1. 开发图形界面
2. 添加音效和动画
3. 联网对战模式
4. 游戏策略分析
---
## 🎯 
"""
import random
def basic_guess_number():
    answer = random.randint(1,100)
    guess_times = 0
    while guess_times < 10:
        guess = int(input('请输入一个在[1,100]之间的整数：'))
        guess_times += 1
        if guess == answer:
            print(f'猜对了，用了{guess_times}次')
            break
        elif guess>answer:
            print('太大了')
        else:
            print('太小了')
    if guess_times==10:
        print('10次机会没有猜中')

if __name__ == '__main__':
    basic_guess_number()